 上层应用协议为了对消息进行区分，往往采用以下方式
1 消息固定长度 累计读取到长度总和后，就认为是一个完整的消息
2 以特殊的分隔符作为消息结束标志 例如ftp 中回车符
3 在消息头中定义长度字段来标识消息长度(这个是比较常用)

第5章包含解码器
DelimiterBasedFrameDecoder 自动完成以分隔符作为结束标志的消息解码
FixedLengthFrameDecoder  对定长消息的解码